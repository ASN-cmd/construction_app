name: construction_field_app
description: Offline-first construction field management app

environment:
  sdk: ">=3.10.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  provider: ^6.0.5
  drift: ^2.15.0
  sqlite3_flutter_libs: ^0.5.15
  geolocator: ^10.1.0
  image_picker: ^1.0.4
  path_provider: ^2.1.2
  path: ^1.9.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  drift_dev: ^2.15.0
  build_runner: ^2.4.8